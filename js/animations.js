/* ============================================================
   VitalEdge Lab — animations.js
   GSAP + ScrollTrigger reveals, hero sequence, counters, parallax.
   Phase 0: stub. Animations added in Phase 7, respecting
   prefers-reduced-motion.
   ============================================================ */
(function () {
  'use strict';

  // Phase 7: register GSAP/ScrollTrigger, build reveals & hero timeline.
})();
